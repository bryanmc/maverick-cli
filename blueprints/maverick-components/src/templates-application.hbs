{{#unless hideLayout}}
    <nav class="navbar navbar-default navbar-static-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#"><strong>{{appName}}</strong></a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li class="active"><a href="#">Home</a></li>
            <li><a href="#about">About</a></li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <li class="active"><a href="#">How to Earn Cash</a></li>
            {{#if isLogged}}
                <li>{{#link-to 'logout'}}Logout{{/link-to}}</li>
            {{else}}
                <li>{{#link-to 'login'}}Sign In{{/link-to}}</li>
            {{/if}}
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </nav>
{{/unless}}

{{outlet}}

{{#unless hideLayout}}
    <footer class="footer hidex" style="bottom:60px;">
      <div class="container">
        <p class="text-muted text-center" style="margin:20px 0">A Milo Coffee Production (@milocoffee)</p>
      </div>
    </footer>
{{/unless}}

{{maverick-editor}}
